import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'intelligent-stock-market-companion',
  webDir: 'www'
};

export default config;
